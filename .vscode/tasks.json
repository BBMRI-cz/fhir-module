{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Blaze stores",
      "type": "shell",
      "command": "docker compose --profile dev up -d test-blaze miabis-blaze",
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    },
    {
      "label": "Create prometheus multiproc dir",
      "type": "shell",
      "command": "mkdir -p ${workspaceFolder}/var/prometheus_multiproc ${workspaceFolder}/var/log/fhir-module",
      "dependsOn": ["Start Blaze stores"],
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
