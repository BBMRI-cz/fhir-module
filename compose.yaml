services:
  fhir-module:
    image: ghcr.io/bbmri-cz/fhir-module:latest
    container_name: fhir-module
    profiles:
      - dev
      - prod
    networks:
      - fhir-integration
      - monitoring
    restart: no
    ports:
      - "5000:5000"
    environment:
      BLAZE_URL: "${BLAZE_URL:-http://test-blaze:8080/fhir}"
      MIABIS_BLAZE_URL: "${MIABIS_BLAZE_URL:-http://miabis-blaze:8080/fhir}"
      MIABIS_ON_FHIR: "${MIABIS_ON_FHIR:-True}"
      DIR_PATH: "${DIR_PATH:-/opt/records}"
      PYTHONWARNINGS: "ignore:Unverified HTTPS request"
    env_file:
      - .env
    volumes:
      - type: bind
        source: "test/xml_data"
        target: "/opt/records"
      - type: bind
        source: "test"
        target: "/opt/test"
      - fhir-logs:/var/log/fhir-module
      - config-snapshots:/opt/config-snapshots

  fhir-ui:
    image: ghcr.io/bbmri-cz/fhir-ui:latest
    container_name: fhir-ui
    profiles:
      - ui
    networks:
      - fhir-integration
      - monitoring
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ui-data:/app/data
    depends_on:
      - fhir-module
    restart: unless-stopped

  fhir-combined:
    image: ghcr.io/bbmri-cz/fhir-combined:latest
    container_name: fhir-combined
    profiles:
      - combined
    networks:
      - monitoring
    restart: unless-stopped
    ports:
      - "5000:5000" # FHIR module port
      - "3000:3000" # UI port
    environment:
      BLAZE_URL: "${BLAZE_URL:-http://localhost:8080/fhir}"
      MIABIS_BLAZE_URL: "${MIABIS_BLAZE_URL:-http://localhost:5432/fhir}"
      MIABIS_ON_FHIR: "${MIABIS_ON_FHIR:-True}"
      DIR_PATH: "${DIR_PATH:-/opt/records}"
      PYTHONWARNINGS: "ignore:Unverified HTTPS request"
    env_file:
      - .env
    volumes:
      - type: bind
        source: "test/xml_data"
        target: "/opt/records"
      - type: bind
        source: "test"
        target: "/opt/test"
      - fhir-logs:/var/log/fhir-module
      - ui-data:/app/data
      - config-snapshots:/opt/config-snapshots
    depends_on:
      - miabis-blaze
      - test-blaze
    healthcheck:
      test:
        [
          "CMD",
          "sh",
          "-c",
          "curl -f http://localhost:3000 && curl -f http://localhost:5000/metrics",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  prometheus:
    image: prom/prometheus:latest
    profiles:
      - monitoring
    networks:
      - monitoring
    ports:
      - "9090:9090"
    volumes:
      - prometheus-config:/etc/prometheus
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.console.libraries=/usr/share/prometheus/console_libraries"
      - "--web.console.templates=/usr/share/prometheus/consoles"

  miabis-blaze:
    container_name: miabis-blaze
    image: "samply/blaze:latest"
    networks:
      - fhir-integration
      - monitoring
    profiles:
      - dev
      - prod
      - combined
    environment:
      JAVA_TOOL_OPTIONS: "-Xmx2g"
      DB_SEARCH_PARAM_BUNDLE: "/app/searchParamBundle.json"
      LOG_LEVEL: "debug"
    ports:
      - "5432:8080"
    volumes:
      - type: bind
        source: "util/searchParamBundle.json"
        target: "/app/searchParamBundle.json"

  test-blaze:
    container_name: test-blaze
    image: "samply/blaze:latest"
    networks:
      - fhir-integration
      - monitoring
    profiles:
      - dev
      - combined
    environment:
      JAVA_TOOL_OPTIONS: "-Xmx2g"
      DB_SEARCH_PARAM_BUNDLE: "/app/searchParamBundle.json"
      LOG_LEVEL: "debug"
    ports:
      - "8080:8080"
    volumes:
      - type: bind
        source: "util/searchParamBundle.json"
        target: "/app/searchParamBundle.json"

  promtail:
    image: grafana/promtail:latest
    container_name: promtail
    profiles:
      - monitoring
    networks:
      - monitoring
    volumes:
      - fhir-logs:/var/log/fhir-module:ro
      - promtail-config:/etc/promtail
    command: -config.file=/etc/promtail/promtail-config.yaml

networks:
  fhir-integration:
  monitoring:

volumes:
  prometheus_data:
  prometheus-config:
  fhir-logs:
  promtail-config:
  ui-data:
  config-snapshots:
